name: workflow
on:
  push:
    branches:
      - bran-secops-lab

jobs:
  SAST:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 1RA PRUEBA
        run: |
          echo "TEST PRUEBA"
          echo "Curso DevSecOps"